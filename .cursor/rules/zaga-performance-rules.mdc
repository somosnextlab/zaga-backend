---
alwaysApply: false
---
🔹 Helpers estándar para respuestas y errores:
- Contenido (crear archivo común):
export type ApiOk<T> = { success: true; data: T };
export type ApiErr = { success: false; error: { code: string; message: string; details?: any } };

export const ok = <T>(data: T): ApiOk<T> => ({ success: true, data });
export const err = (code: string, message: string, details?: any): ApiErr => ({
  success: false, error: { code, message, details },
});

- Usar en todos los servicios/controladores.
- Nunca devolver raw DB models directamente.